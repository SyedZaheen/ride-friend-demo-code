import notDefined from "./notDefined.mjs";

function gatherData() {
  //This function queries the database and creates a list that can be passed on as an argument
  //to the User constructor to create a User object that can be user for Javascript operations
  notDefined(gatherData);
}

