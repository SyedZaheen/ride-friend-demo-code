import notDefined from "../notDefined.mjs";

function gatherRawData() {
  //This function queries the database and creates a list that can be passed on as an argument
  //to the User constructor to create a User object that can be used for Javascript operations
  notDefined(gatherRawData);
}
